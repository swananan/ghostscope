interface:
  display_name: "GhostScope E2E Runner"
  short_description: "Run GhostScope e2e via runner service or container scripts"
  default_prompt: "Use this skill to run GhostScope e2e tests through scripts/e2e and report results."
